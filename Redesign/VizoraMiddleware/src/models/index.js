/**
 * Models index - exports all models from a single file
 */

const Display = require('./Display');
const Controller = require('./Controller');
const User = require('./User');

module.exports = {
  Display,
  Controller,
  User
}; 