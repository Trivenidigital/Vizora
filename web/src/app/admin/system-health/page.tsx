import SystemHealthClient from './page-client';

export const metadata = {
  title: 'System Health Monitor | Vizora Admin',
};

export default function SystemHealthPage() {
  return <SystemHealthClient />;
}
